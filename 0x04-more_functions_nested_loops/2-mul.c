#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * mul - multiples two integers
 *
 * @a: integer
 * @b: integer
 * Return: 0
 */

int mul(int a, int b)
{
	int multi;
	
	if (a >= 0 && a < 10 && b >= 0 && b < 10)
{
	multi = a * b
	printf("%d", multi);
	getch();
}
}
