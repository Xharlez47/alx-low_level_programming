#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * mul - multiples two integers
 *
 * @a: integer
 * @b: integer
 * Return: 0
 */
int mul(int a, int b)
{
	int multi;

	if (a >= 58 && b <= 57)
	{
	else if (b >= 58 &&bc <= 57)
	{
		multi = a * b;
	}
		putchar(multi);
	}
	return (0);
}
